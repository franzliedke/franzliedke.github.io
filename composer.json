{
    "require": {
        "tightenco/jigsaw": "^1.0"
    },
    "scripts": {
        "build": "jigsaw build",
        "watch": "npm run watch",
        "build-production": "npm run prod && jigsaw build production"
    },
    "config": {
        "process-timeout": 0
    }
}
